import { typography } from './typography';
import { white, gray, black, blue, primary } from "./palette";

const colors = {
    white,
    gray,
    black,
    blue,
    primaries: primary,
}

const base = {
    smallBorderRadius: 6,
    borderRadius: 8,
    largeBorderRadius: 12,
};

export default {
    colors,
    base,
    typography,
}